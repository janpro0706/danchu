// for nested components to be imported in app.module.ts

import { LoginPage } from './login';

const declarations = [ LoginPage ];
const entryComponents = [ LoginPage ];

export default { declarations, entryComponents };
